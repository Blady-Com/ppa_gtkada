with "gtkada.gpr";

project Image_Display is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("image_display.adb");

   package Compiler is
      for Switches ("ada") use ("-gnatwa");
   end Compiler;

end Image_Display;

